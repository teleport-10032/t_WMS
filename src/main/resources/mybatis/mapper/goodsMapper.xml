<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.t_wms.mapper.goodsMapper">
    <select id="getGoodsList" resultType="goods">
        select goods.id,goodsName,typeName,unit,info,price
            from goods,type where type.id = goods.typeId and goodsName like "%"#{key}"%"
            order by id limit #{start},#{num};
    </select>

    <select id="getGoodsNum" resultType="int">
        select count(id) from goods
    </select>
</mapper>
